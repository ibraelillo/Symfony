{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Reservacion creation</h1>

    <form action="{{ path('location_{id}_reservaciones_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('location_{id}_reservaciones') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
